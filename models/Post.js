class Post {
  constructor(id, title, body, user_location_id) {
    this.id = id;
    this.title = title;
    this.body = body;
    this.user_location_id = user_location_id;
  }
}

export default Post;
